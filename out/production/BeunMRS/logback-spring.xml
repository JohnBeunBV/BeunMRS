<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console appender — human-readable for local dev -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Loki appender — structured JSON pushed to Grafana Loki -->
    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>${LOKI_URL:-http://loki:3100/loki/api/v1/push}</url>
        </http>
        <format>
            <label>
                <pattern>app=notification-svc,host=${HOSTNAME},level=%level</pattern>
            </label>
            <message>
                <pattern>
                    level=%level logger=%logger{36} thread=%thread message="%msg"%n
                </pattern>
            </message>
        </format>
    </appender>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOKI"/>
    </root>

    <!-- Verbose logging for our own code -->
    <logger name="com.openmrs.notification" level="DEBUG"/>

    <!-- Reduce noise from Spring internals -->
    <logger name="org.springframework.amqp" level="INFO"/>
    <logger name="org.springframework.web"  level="WARN"/>
    <logger name="com.zaxxer.hikari"        level="WARN"/>

</configuration>
